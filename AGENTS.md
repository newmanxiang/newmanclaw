# AGENTS.md

## Cursor Cloud specific instructions

This is an empty/skeleton repository (`newmanclaw`) with no application code, dependencies, or services. The repo contains only a `README.md`.

- **No build/lint/test commands** are available — there is no source code or tooling configured.
- **No services** need to be started.
- **No dependencies** need to be installed.

When application code is added to this repository, this section should be updated with relevant development instructions (how to install dependencies, run services, lint, test, and build).
